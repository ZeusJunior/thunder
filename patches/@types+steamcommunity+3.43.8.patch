diff --git a/node_modules/@types/steamcommunity/components/webapi.d.ts b/node_modules/@types/steamcommunity/components/webapi.d.ts
index fc7ac8f..bc95369 100644
--- a/node_modules/@types/steamcommunity/components/webapi.d.ts
+++ b/node_modules/@types/steamcommunity/components/webapi.d.ts
@@ -39,4 +39,14 @@ export interface WebApi {
      * @param callback A function to be called once the token is obtained.
      */
     getWebApiOauthToken(callback: Callback): any;
+
+		/**
+		 * Sets an access_token generated by steam-session using EAuthTokenPlatformType.MobileApp.
+		 * Required for some operations such as 2FA enabling and disabling.
+		 * This will throw an Error if the provided token is not valid, was not generated for the MobileApp platform, is expired,
+		 * or does not belong to the logged-in user account.
+		 *
+		 * @param {string} token
+		 */
+		setMobileAppAccessToken(token: string): void;
 }
diff --git a/node_modules/@types/steamcommunity/index.d.ts b/node_modules/@types/steamcommunity/index.d.ts
index 65ab085..9011da7 100644
--- a/node_modules/@types/steamcommunity/index.d.ts
+++ b/node_modules/@types/steamcommunity/index.d.ts
@@ -43,6 +43,8 @@ interface SteamCommunity
 declare class SteamCommunity {
     constructor(options?: SteamCommunity.Options);
 
+		mobileAccessToken?: string;
+
     /**
      * Invalidates your account's existing trade URL and generates a new token, which is returned in the callback.
      *
